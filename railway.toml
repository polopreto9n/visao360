# railway.toml — configuracoes de deploy compartilhadas
# Dockerfile path configurado por servico no dashboard do Railway:
#   visao360-web -> apps/web/Dockerfile
#   visao360-api -> Dockerfile (raiz)

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
